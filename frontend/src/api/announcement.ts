import {request} from '../utils/request';import type{Announcement}from '../types';export const listAnnouncements=()=>request<Announcement[]>('/announcements');export const createAnnouncement=(data:Pick<Announcement,'title'|'content'|'category'|'top'>)=>request<Announcement>('/announcements',{method:'POST',body:JSON.stringify(data)});export const detailAnnouncement=(id:number)=>request<Announcement>(`/announcements/${id}`);
