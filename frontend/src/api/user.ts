import {request} from '../utils/request';import type{User}from '../types';export const login=(phone:string,password:string)=>request<{token:string;user:User}>('/auth/login',{method:'POST',body:JSON.stringify({phone,password})});export const getMe=()=>request<User>('/users/me');export const updateMe=(data:Partial<User>)=>request<User>('/users/me',{method:'PUT',body:JSON.stringify(data)});export const getStaff=()=>request<User[]>('/users/staff');
