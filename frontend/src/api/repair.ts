import {request} from '../utils/request';import type{Repair,RepairStatus}from '../types';export const listRepairs=(status?:string)=>request<Repair[]>(`/repairs${status?`?status=${status}`:''}`);export const createRepair=(data:Pick<Repair,'title'|'description'|'type'|'images'>)=>request<Repair>('/repairs',{method:'POST',body:JSON.stringify(data)});export const assignRepair=(id:number,handler_id:number)=>request<Repair>(`/repairs/${id}/assign`,{method:'PATCH',body:JSON.stringify({handler_id})});export const updateRepairStatus=(id:number,status:RepairStatus)=>request<Repair>(`/repairs/${id}/status`,{method:'PATCH',body:JSON.stringify({status})});
