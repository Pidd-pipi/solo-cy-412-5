<template><article class="card repair-card"><div><h3>{{repair.title}}</h3><p>{{repair.description}}</p><small>{{repair.type}} · {{repair.user?.nickname||'业主'}} · {{new Date(repair.created_at).toLocaleString()}}</small></div><RepairStatusBadge :status="repair.status"/></article></template><script setup lang="ts">import type{Repair}from'../../types';import RepairStatusBadge from'./RepairStatusBadge.vue';defineProps<{repair:Repair}>()</script>
