import type {ApiResponse} from '../types'; export async function request<T>(url:string,options:RequestInit={}){const token=localStorage.getItem('smartestate_token');const r=await fetch(`/api/v1${url}`,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}});const body=await r.json() as ApiResponse<T>;if(!r.ok||body.code!==0)throw new Error(body.message||'请求失败');return body.data}
